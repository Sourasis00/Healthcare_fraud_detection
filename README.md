# Healthcare_fraud_detection
Built an ML-based healthcare fraud detection system using an enhanced insurance claims dataset. Performed preprocessing, feature engineering, and class balancing, and trained a Random Forest model to identify fraudulent claims. Evaluated using accuracy, ROC-AUC, and deployed the model for prediction.
